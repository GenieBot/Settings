GenieBot's Settings Module

Settings module provides the ability to change and reset the bot's default prefix.

Usage: `prefix [network/channel] [prefix]` where prefix is a string between 1 and 21 characters inclusive in length.
======
See the [GenieBot](https://github.com/GenieBot) org for more modules
